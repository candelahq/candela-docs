# yaml-language-server: $schema=https://json.schemastore.org/lefthook.json

pre-commit:
  parallel: true
  commands:
    yaml-lint:
      glob: "*.{yml,yaml}"
      run: nix develop -c yamllint {staged_files}
    mkdocs-build:
      run: nix develop -c uv run mkdocs build --strict 2>&1 | tail -5
